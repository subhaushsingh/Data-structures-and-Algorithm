{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run C++ file (bash)",
      "type": "shell",
      "command": "g++ -std=c++17 -o \"${fileDirname}/${fileBasenameNoExtension}\" \"${file}\" && \"${fileDirname}/${fileBasenameNoExtension}\" < \"${fileDirname}/input.txt\" > \"${fileDirname}/output.txt\"",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Compile and run C++ code with input and output redirection (bash)"
    }
  ]
}
